[build-system]
requires = ["setuptools>=61.0", 'pandas', 'fire', 'numpy', "pysam",
    'biopython']
build-backend = "setuptools.build_meta"

[tool.setuptools_scm]
version_scheme = "guess-next-dev"
write_to = 'czip/_version.py'